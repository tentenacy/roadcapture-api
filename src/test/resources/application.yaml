spring:
  profiles:
    active: test
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://rc-instance.chliztzahdc3.ap-northeast-2.rds.amazonaws.com:3306/mysql?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: create
    generate-ddl: false
    show_sql: true
  data:
    web:
      pageable:
        default-page-size: 10
        max-page-size: 50


server:
  error:
    include-exception: true      # Response 에 Exception 을 표시할지
    include-message: always       # Response 에 Exception Message 를 표시할지 (never | always | on_param)
    include-stacktrace: never  # Response 에 Stack Trace 를 표시할지 (never | always | on_param) on_trace_params 은 deprecated
    whitelabel.enabled: true      # 에러 발생 시 Spring 기본 에러 페이지 노출 여부

logging:
  level:
    org.hibernate: debug